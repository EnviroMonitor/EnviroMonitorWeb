language: python

python:
  - "2.7"
  - "3.5"

sudo: required

services:
  - docker

# to build container
install:
  - docker-compose build web

# command to run tests
script:
  - docker-compose run web py.test
